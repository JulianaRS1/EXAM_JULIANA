import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exam_juliana',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
